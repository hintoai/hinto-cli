---
"@hintoai/cli": patch
---

Upgrade Biome to v2 (config migrated to the v2 schema). Removed unused imports and
fixed a `forEach` callback flagged by the stricter v2 recommended rules. No runtime
behavior change.

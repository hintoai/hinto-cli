---
'@hintoai/cli': minor
---

Add article brief support: `--brief` on `articles create`, `articles create-empty`, `articles update`, and `generate start`; `--clear-brief` on `articles update`; `--brief-addition` on `articles regenerate`. `articles get` now shows the brief and `articles versions` shows each run's change request.

---
"@hintoai/cli": patch
---

Skill: make the npm package name (`@hintoai/cli`) unmissable and add explicit
upgrade instructions, so agents stop guessing the non-existent `@hinto/cli` when
asked to install or upgrade.
